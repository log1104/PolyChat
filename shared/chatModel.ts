/**
 * Single source of truth for the default chat LLM.
 * Update this value to change both the UI's initial selection and the backend fallback.
 */
export const DEFAULT_CHAT_MODEL = "openai/gpt-4o-mini";
